APIFOO/VIEWS

import requests
from django.shortcuts import render

def home(request):
    quotes = []
    error = None

    if request.method == "POST":
        num = request.POST.get("num")

        try:
            if not num:
                response = requests.get("https://stoic-quotes.com/api/quote")
                quotes = [response.json()]
            else:
                response = requests.get(f"https://stoic-quotes.com/api/quotes?num={num}")
                quotes = response.json()

        except:
            error = "Unable to retrieve quotes at the moment. Please try again later."

    return render(request, "home.html", {
        "quotes": quotes,
        "error": error
    })

APIFOO/URLS
from django.urls import path
from . import views

urlpatterns = [
    path('', views.home, name='home'),
]

PROJECTSITE/URLS

from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('apifoo.urls')),
]
TEMPLATES/HOME:

<!DOCTYPE html>
<html>
<head>
    <title>Stoic Quotes</title>
</head>
<body>

    <h1>Stoic Quotes Generator</h1>

    <form method="POST">
        {% csrf_token %}
        <input type="number" name="num" placeholder="Enter number of quotes">
        <button type="submit">Get Quotes</button>
    </form>

    <hr>

    {% if error %}
        <p style="color:red;">{{ error }}</p>
    {% endif %}

    {% for quote in quotes %}
        <p><strong>"{{ quote.text }}"</strong></p>
        <p>- {{ quote.author }}</p>
        <hr>
    {% endfor %}

</body>
</html>

PROJECTSITE/SETTINGS
'DIRS': [BASE_DIR / 'templates'],

pip install -r requirements.txt
pip install requests
python -m pip install requests
py --version